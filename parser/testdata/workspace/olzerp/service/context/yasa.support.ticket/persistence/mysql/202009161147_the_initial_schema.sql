CREATE TABLE book
(
    `id` BINARY(16),
    name VARCHAR(255)
);

CREATE TABLE book3
(
    id BINARY(16)
);

CREATE TABLE book3
(
    id JSON
);

SELECT blub
from BLA;

INSERT INTO book3
VALUES (1, 2);

ALTER TABLE BLA
    ADD COLUMN (id TEXT);